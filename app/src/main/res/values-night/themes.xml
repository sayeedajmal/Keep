<resources>
    <!-- Base application theme. -->
    <style name="Theme.Keep" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:fontFamily">@font/trebuc</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:colorBackground">#252c33</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:screenOrientation">portrait</item>
        <item name="android:statusBarColor">@color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="cardBackgroundColor">#A3009688</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="DialogAnim">
        <item name="android:windowEnterAnimation">@anim/slide_to_bottom</item>
        <item name="android:windowExitAnimation">@anim/slide_to_up</item>
    </style>
</resources>