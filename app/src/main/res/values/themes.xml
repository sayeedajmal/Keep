<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Keep" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:fontFamily">@font/trebuc</item>
        <item name="android:textColor">@color/black</item>
        <item name="color">@color/white</item>
        <item name="android:colorBackground">#F2FFFFFF</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:screenOrientation">portrait</item>
        <item name="android:statusBarColor">#F2FFFFFF</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/lightGray</item>
        <item name="cardBackgroundColor">@color/lightGray</item>
        <item name="android:windowTranslucentStatus">true</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="DialogAnim">
        <item name="android:windowEnterAnimation">@anim/slide_to_bottom</item>
        <item name="android:windowExitAnimation">@anim/slide_to_up</item>
    </style>
</resources>